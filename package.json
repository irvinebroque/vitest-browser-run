{
	"name": "@vitest-browser-run/example-worker",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@10.26.1",
	"scripts": {
		"build:provider": "pnpm --filter @vitest-browser-run/browser-run-provider build",
		"build": "vite build",
		"deploy": "pnpm build && wrangler deploy",
		"dev": "vite dev",
		"preview": "pnpm build && vite preview",
		"start": "vite dev",
		"test": "pnpm --filter @vitest-browser-run/browser-run-provider test",
		"test:worker": "vitest run --config vitest.config.ts",
		"test:browser-run": "pnpm build:provider && vitest run --config vitest.browser-run.config.ts",
		"test:browser-run:visual": "pnpm build:provider && vitest run --config vitest.browser-run.config.ts test/browser/visual-*.browser.test.ts",
		"test:browser-run:visual:update": "pnpm build:provider && vitest run --config vitest.browser-run.config.ts test/browser/visual-*.browser.test.ts --update",
		"typecheck": "pnpm build:provider && tsc --noEmit --project tsconfig.json && tsc --noEmit --project test/tsconfig.json && tsc --noEmit --project test/browser/tsconfig.json && pnpm --filter @vitest-browser-run/browser-run-provider typecheck",
		"test:watch": "vitest --config vitest.config.ts",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@vitest-browser-run/browser-run-provider": "workspace:*",
		"@cloudflare/vite-plugin": "1.42.4",
		"@cloudflare/vitest-pool-workers": "0.17.0",
		"@types/node": "26.1.0",
		"@vitest/browser": "link:../vitest/packages/browser",
		"@vitest/browser-playwright": "link:../vitest/packages/browser-playwright",
		"playwright": "npm:playwright-core@1.61.1",
		"typescript": "5.9.3",
		"vite": "8.1.2",
		"vitest": "link:../vitest/packages/vitest",
		"wrangler": "4.106.0"
	}
}
